# Example environment variables for local development.
# Copy to `.env.local` and fill in values as needed.
#
# This project currently runs without any required env vars,
# but the keys below are common defaults you may adopt.

# Public (available in the browser)
NEXT_PUBLIC_APP_NAME="IBR and Courses"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

